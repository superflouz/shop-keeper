using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyGenerator : MonoBehaviour
{
    // Create multiple enemy party
    // Each party represents a world or a wave (?)
    // Select entities to spawn randomly on a range in the list that coresponds to the current world/progress

    // List of all enemies
    public List<Entity> Enemies;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
